# Raspberry Pi Docker Container for FHEM

Docker image for FHEM based on resin/rpi-raspbian:jessie

FHEM source files and config will persist after build in ./fhem

# Build

docker-compose build

# Start

docker-compose up -d

